{% extends 'base.nav.html.twig' %}

{% block title %}Date pentru: {{ user.username }}{% endblock %}

{% block body %}
  <h2 class="coinyFont">{{ user.getRoName }}</h2>
  <p>E-mail: {{ user.email }}</p>
  <p>Tip Utilizator: {{ user.usertype }}</p>
  <br>
  <br>
  <a href="{{ path('users') }}"><button type="button" class="btn btn-primary">Back...</button></a>
{% endblock %}
