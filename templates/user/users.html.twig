{% extends 'base.nav.html.twig' %}

{% block title %}Administrare Utilizatori{% endblock %}

{% block body %}
  {% if users %}
    <table class="table table-striped table-hover">

      <thead>
        <tr>
          <th>Username</th>
          <th>E-mail</th>
          <th>Type</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        {% for user in users %}
          <tr>
            <td>{{ user.username }}</td>
            <td>{{ user.email }}</td>
            <td>{{ user.usertype }}</td>
            <td><a href="{{ path('user_show', {'id':user.id}) }}"><button type="button" class="btn btn-primary">Edit...</button></a></td>
          </tr>
        {% endfor %}
        <!-- <tr>
          <td>John</td>
          <td>john@example.com</td>
          <td>Admin</td>
          <td><button class="button">Edit...</button></td>
        </tr>
        <tr>
          <td>John</td>
          <td>john@example.com</td>
          <td>Admin</td>
          <td><button class="button">Edit...</button></td>
        </tr>
        <tr>
          <td>John</td>
          <td>john@example.com</td>
          <td>Admin</td>
          <td><button class="button">Edit...</button></td>
        </tr> -->
      </tbody>

    </table>
  {% else %}
    <p>No users found in DB</p>
  {% endif %}

{% endblock %}
